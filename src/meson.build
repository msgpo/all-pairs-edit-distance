all_pairs_edit_distance_sources = [
  'main.cpp',
]

all_pairs_edit_distance_deps = [
]

executable('all-pairs-edit-distance', all_pairs_edit_distance_sources,
  cpp_args: ['-fopenmp'],
  link_args: ['-fopenmp'],
  dependencies: all_pairs_edit_distance_deps,
  install: true,
)
